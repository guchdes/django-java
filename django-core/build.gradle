plugins {
    id "com.github.hierynomus.license"
}

version = "${projectVersion}"


dependencies {
    compile project(":django-bson")
}

license {
    header rootProject.file('LICENSE')
    include "**/*.java"
}